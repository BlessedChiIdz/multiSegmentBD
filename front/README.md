# Frontend (multiSegmentBD)

Стек: **React** + **TypeScript** + **Material UI**.

http://localhost:3000 Запросы к API проксируются на `http://127.0.0.1:8080` (см. `"proxy"` в `package.json`).

Другой URL API:

Селект руками писать, добавить индексы и прочее...
Авторизация!!!!!!!!!
переделать на табличный вариант при показе результатов